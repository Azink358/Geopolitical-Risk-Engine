mappings:
  # Dependency schema
  fuel_type_key: fuel_type_key

  # Common keys
  country_key: country_key
  route_key: route_key
  vessel_type_key: vessel_type_key
  conflict_phase_key: conflict_phase_key
  date_key: date_key

  # Shipping disruptions enrichment
  event_key: event_key
  incident_type: incident_type
  severity_level: severity_level
  flag_state: flag_state
  reroute_required: reroute_required
  delay_hours: delay_hours
  cargo_volume: cargo_volume
  latitude: latitude
  longitude: longitude
